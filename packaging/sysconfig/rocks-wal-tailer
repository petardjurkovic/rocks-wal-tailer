# Global defaults. Per-instance overrides go in /etc/sysconfig/rocks-wal-tailer-<instance>

# Example:
# SRC="srcdb.kv,srcdb.other"
SRC="srcdb.kv"

# ClickHouse HTTP endpoint
CH_URL="http://127.0.0.1:8123"

# Optional auth (your binary already reads CH_USER/CH_PASSWORD envs)
CH_USER="default"
CH_PASSWORD="default123"

# Destination
CH_DATABASE="wal"
CH_TABLE="rdb_changelog"

# Where checkpoints live (per-instance default below)
CHECKPOINT_DIR="/var/lib/rocks-wal-tailer/%i"

# Node identifier (overrides --node-id in the binary if set)
NODE_ID="$(hostname -f 2>/dev/null || hostname)"
